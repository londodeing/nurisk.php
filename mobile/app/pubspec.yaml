name: nurisk_mobile
description: NURISK Mobile - Public First Architecture
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^3.3.2
  go_router: ^17.3.0
  dio: ^5.4.3
  cached_network_image: ^3.3.1
  drift: ^2.18.0
  sqlite3_flutter_libs: ^0.6.0+eol
  flutter_dotenv: ^6.0.1
  maplibre_gl: ^0.26.2
  http: ^1.2.1
  shared_preferences: ^2.3.4
  path_provider: ^2.1.5
  permission_handler: ^12.0.3
  geolocator: ^14.0.3
  image_picker: ^1.1.2

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_secure_storage: ^10.3.1
  google_fonts: ^6.2.1
  flutter_svg: ^2.0.17
  open_filex: ^4.5.0
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  riverpod_generator: ^4.0.4
  build_runner: ^2.4.9
  drift_dev: ^2.18.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  #   fonts:
  #     - family: Schyler
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #     - family: Trajan Pro
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  assets:
    - assets/profil/
    - assets/login/
    - assets/logo/
    - assets/master/version.json
    - assets/master/bencana/jenis.json
    - assets/master/status/laporan.json
    - assets/master/status/insiden.json
    - assets/master/status/operasi.json
    - assets/master/severity/master.json
    - assets/master/prioritas.json
    - assets/master/klaster/data.json
    - assets/master/relawan/jenis.json
    - assets/master/logistik/jenis.json
    - assets/master/satuan.json
    - assets/master/skala_kejadian.json
    - assets/master/level_risiko.json
    - assets/master/resource/jenis.json
    - assets/master/kendaraan/jenis.json
    - assets/master/shelter/jenis.json
    - assets/master/warna_indikator.json
    - assets/master/icon_cop/data.json
    - assets/master/surat/jenis.json
    - assets/master/surat/jabatan_ttd.json
    - assets/master/assessment/indikator.json
    - assets/master/assessment/kebutuhan_numerik.json
    - assets/master/assessment/indikator_skor_panduan.json
    - assets/master/approval/status.json
    - assets/master/workflow/enum.json
    - assets/master/wilayah/kabupaten.json
    - assets/master/wilayah/kecamatan.json
    - assets/master/wilayah/desa.json
    - assets/master/wilayah/master.db
